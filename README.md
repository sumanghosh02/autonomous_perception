# Robust Autonomous Car Perception System

## Features
- Real-time object detection (YOLOv8)
- Lane detection (OpenCV)
- Adverse-condition enhancement (CLAHE)
- Security simulation (noise attack)
- Robustness demo (failure + recovery)
- FPS display

## Setup
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt